<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
    http://www.springframework.org/schema/beans/spring-beans.xsd
    http://code.alibabatech.com/schema/dubbo
    http://code.alibabatech.com/schema/dubbo/dubbo.xsd">

    <!-- 消费方应用名，用于计算依赖关系，不是匹配条件，不要与提供方一样 -->
    <dubbo:application name="${dubbo.application.provider.name}"/>

    <!-- 使用redis注册中心暴露发现服务地址 -->
    <!--<dubbo:registry address="redis://127.0.0.1:6379"/>-->

    <!-- 使用zookeeper注册中心暴露发现服务地址 -->
    <dubbo:registry protocol="${dubbo.registry.protocol}" address="${dubbo.registry.address}"/>
    <!--<dubbo:registry protocol="zookeeper" address="127.0.0.1:2181"/>-->

    <!-- 使用广播注册中心暴露服务地址 -->
    <dubbo:protocol name="dubbo" port="20881" />
    <!-- 生成远程服务代理-->
    <dubbo:service interface="com.ewell.emr.application.service.app1.IOrderService" ref="iOrderService"></dubbo:service>

    <bean id="iOrderService" class="com.ewell.emr.application.service.app1.impl.OrderServiceImpl"></bean>


</beans>